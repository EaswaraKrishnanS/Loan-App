import React from "react";
import { Typography } from "@mui/material";

const About = () => (
  <Typography variant="body1">
    This Loan Calculator App is built using React and Material UI. It supports
    EMI calculations, real-time exchange rates, light/dark themes, and is
    responsive on all devices.
  </Typography>
);

export default About;
